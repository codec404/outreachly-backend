services:
  chat-service:
    container_name: chat-service
    build:
      context: .
      target: prod
    ports:
      - "8080:8080"
    env_file:
      - local.env
    environment:
      APP_ENV: production
    restart: unless-stopped
